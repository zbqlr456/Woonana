server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://k6b104.p.ssafy.io:3306/woonana
    username: root
    password: woonana123@
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    generate-ddl: false
    open-in-view: false
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
  devtools:
    livereload:
      enabled: true
    restart:
      enabled: false
  servlet:
    multipart:
      max-file-size: 20MB
      max-request-size: 20MB

logging.level:
  org.hibernate.SQL: debug  # ?? hibernate? ???? SQL? ??? ??? ??? ? ??(?????? ??!)
  org.hibernate.type: trace

kakao.login:
  client_id: 76d85e088d5bb875d6e4493b8eda78fd
#  redirect_url: https://k6b104.p.ssafy.io/oauth/callback
  redirect_url: http://localhost:8080/oauth/callback

cloud:
  aws:
    s3:
      bucket: woonana
    region:
      static: ap-northeast-2
    stack:
      auto: false
    credentials:
      instanceProfile: true
